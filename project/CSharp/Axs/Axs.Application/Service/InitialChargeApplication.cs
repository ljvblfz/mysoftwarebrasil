using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;
using Axis.Domain;

namespace Axis.Application
{
    public class InitialChargeApplication
    {
        public InitialChargeApplication()
        {
        }

        public static void Start()
        {
            //new GenericRepository<Cabelo>().Save(Cabelo.List());
            //new GenericRepository<EstadoCivil>().Save(EstadoCivil.List());
            //new GenericRepository<Etinia>().Save(Etinia.List());
            //new GenericRepository<Olho>().Save(Olho.List());
            //new GenericRepository<Sexo>().Save(Sexo.List());
            //new GenericRepository<Estado>().Save(Estado.List());
            //new GenericRepository<Cidade>().Save(Cidade.List());
        }
    }
}